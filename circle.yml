machine:
  services:
    - docker

dependencies:
  override:
    - sbt compile

dependencies:
  cache_directories:
    - "~/.ivy2"

deployment:
  releases: 
    branch: /1\.0\.*/
    commands:
      - sbt docker:publishLocal
