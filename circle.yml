machine:
  services:
    - docker

dependencies:
  override:
    - sbt compile

dependencies:
  cache_directories:
    - "~/.ivy2"

deployment:
  releases:
    branch: /1.*/
    commands:
      - sbt docker:publishLocal
          timeout: 30
